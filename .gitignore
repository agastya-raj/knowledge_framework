__pycache__/
*.pyc
.DS_Store

# Ephemeral audit breadcrumbs (processed by weekly job)
_audit_queue/*.md

# Audit logs
scripts/*.log

# Bootstrap should never commit these (they live in ~/.claude/)
# settings.local.json — machine-specific, never synced
# .settings_template_hash — generated at bootstrap time
